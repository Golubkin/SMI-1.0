T absExample(T x)
{
    if (x >= 0)
        return x;
    else
        return -x;
}

// Но при реализации шаблона в функции нам придётся ещё и дополнительно писать код, в котором будем указывать, что 
// вместо T может быть либо int, либо double. Потому что, например, char или bool нам тут не нужен. Это только усложнит код.
// Мне кажется, можно обойтись просто double (как и написал в mainfunc.cpp).
